name: Common Cron Checks

on:
  schedule:
    - cron: "0 0 * * *" # Runs daily at 00:00 UTC

permissions: write-all

jobs:
  common-cron-checks:
    uses: neteye-platform/repo-commons/.github/workflows/common-cron-checks.yaml@f54c74169b4fa5420c860a9cc534f3554ba47577 # main
    secrets: inherit
